/**
 * Makerspace orientation clip (local file, not YouTube).
 * Place the file at `public/video.mp4` — served as `/video.mp4`.
 */
export const MAKERSPACE_GUIDE_VIDEO_PATH = "/video.mp4"

/** Builder header link — same asset, opens in a new tab. */
export const MAKERSPACE_GUIDE_VIDEO_URL = MAKERSPACE_GUIDE_VIDEO_PATH
