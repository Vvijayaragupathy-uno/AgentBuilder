server {
    gzip on;
    gzip_comp_level  2;
    gzip_min_length  1000;
    gzip_types  text/xml text/css;
    gzip_http_version 1.1;
    gzip_vary  on;
    gzip_disable "MSIE [4-6] \.";

    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html =404;
    }

    # Proxy API requests to the backend.
    # __BACKEND_SCHEME__ is set by start-nginx.sh to either "http" or "https"
    # depending on whether BACKEND_URL is an internal or public address.
    location /api/ {
        proxy_pass __BACKEND_URL__;
        proxy_http_version 1.1;

        # SNI — only meaningful for HTTPS upstreams; nginx ignores for HTTP.
        proxy_ssl_server_name __PROXY_SSL_SERVER_NAME__;
        proxy_ssl_protocols TLSv1.2 TLSv1.3;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $proxy_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Local health check
    location ~ ^/(health|health_check)$ {
        return 200 'OK';
        add_header Content-Type text/plain;
    }

    include /etc/nginx/extra-conf.d/*.conf;
}
